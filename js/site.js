// Implement new day, add item and cross out item
var list = {
  name: "Justin",
  transfer: FALSE,
  big/medium/small:{
    datestamp: ["task", boolStatus]
  }
  newDay: function () {
    if transfer = true {
      clear all true status tasks.
    } else {
      clear all tasks
    }
  }
  addTask:
  toggleStatus:
  saveList: {
    save to local storage
  }
}

// $( document ).ready(function(){
//
// });
